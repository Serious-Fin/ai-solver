services:
  api:
    build: ./api
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./api/data:/app/data
    expose:
      - "8080"
  frontend:
    build: ./frontend
    ports:
      - "80:3000"